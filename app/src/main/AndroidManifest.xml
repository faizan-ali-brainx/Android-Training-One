<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.brainx.baseproject">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".base.BaseApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidBaseCode"
        tools:replace="android:label,android:allowBackup">
        <activity
            android:name=".activaties.ScopeStorage"
            android:exported="true" />
        <activity
            android:name=".activaties.ScopedStorageActivity"
            android:exported="true" />
        <activity
            android:name=".activaties.WorkManagerActivity"
            android:exported="true" />
        <activity
            android:name=".activaties.SecondActivity"
            android:exported="true" />
        <activity
            android:name=".activaties.FirstActivity"
            android:exported="true" />
        <activity
            android:name=".activaties.ThirdActivity"
            android:exported="true" />
        <activity
            android:name=".activaties.BaseActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>